# AGENTS.md

## 项目背景

- 当前项目基于 STC AI8051U 平台开发。
- 编译器为 Keil5 C251，代码需遵循 C89 标准。
- 这是一个高速电磁循迹、差速转向、负压三轮小车项目。
- 小车通过四路电感传感器检测环境中的电磁线，包含两路横向电感和两路竖向电感。
- 通过 PWM 控制两个电机驱动，实现自动循迹。
- 赛道包含折线、直角转弯、圆环、菱形、直立墙面和立体桶等复杂元素。
- 当前参赛组别为智能车竞赛“飞檐走壁组”。

## 开发目标

- 优先保证执行效率、实时响应速度和控制链路稳定性。
- 在高速循迹场景下尽量减少延迟、抖动和误判。
- 避免因传感器处理不及时、控制链路过慢而丢失电磁线。
- 在保证性能的前提下，保持模块清晰、注释必要、便于调试和现场排障。

## 基本工作原则

- 开始修改前，先梳理当前项目和相关模块，不要脱离现有实现做臆测式修改。
- 每次修改后，都要检查是否改到位，确认行为、接口和影响范围与预期一致。
- 优先复用现有函数、变量、常量和控制链路，避免功能等价但重复的新实现。
- 修改和新增代码都要坚持最小实现，避免啰嗦、重复和过度抽象。
- 默认限制调用链和阅读跳转深度：普通逻辑最多三层跳转；除非问题本身十分复杂或能明显降低重复，否则不要增加只做转发的包装函数，能直接写清楚就直接写。
- 单次调用、逻辑很短、不会复用的小函数不要拆出来；优先内联到调用处，减少跳转。
- 不要用过度抽象换取“看起来更面向对象”的结构，尤其不要为了形式上的解耦增加大量简单 getter、wrapper 或只转发一层的接口。
- 如果结构优化会损伤实时性，应优先保留实时性，并明确说明取舍。

## 编码与工具约束

- 所有代码必须符合 C89 标准，禁止使用 C99、C11 或更新标准中的语法与特性。
- 修改代码时优先考虑 Keil5 C251 的兼容性。
- 可以使用 `int32`，但仍需注意目标平台上的类型宽度和运算开销。
- `AGENTS.md` 与包含中文注释或字符串的源码文件统一使用 UTF-8 编码；若遇到 Keil5 C251 的特殊兼容性问题，以可编译和可读性为先。
- 编译工具路径为 `D:\keil_5\UV4\UV4.exe`。
- 头文件统一通过 `#include "zf_common_headfile.h"` 管理，避免分散包含造成依赖失控。
- 读取日志相关的文件统一放在 `log/` 目录下，不要分散存放。
- Git 提交信息要求使用中文。
- 代码风格尽量遵循 Google C 编程规范，但以 C89 兼容、Keil5 C251 兼容和实时性优先为前提。

## 代码修改要求

1. 确保代码符合 C89 标准，避免使用不兼容特性，例如混合声明与语句、`for (int i = ...)`、`inline`、`stdbool.h` 和指定初始化。
2. 优化循环、分支和数据路径，避免不必要的浮点运算、重复计算、频繁除法、动态分配及其他高开销操作。
3. 及时更新传感器数据，尽量缩短“采样-计算-控制输出”链路，避免因处理延迟导致循迹错误。
4. 控制相关逻辑优先考虑实时性，避免在高频路径中加入打印、阻塞等待或重型调试逻辑。
5. 增加必要且简洁的注释，重点说明状态机、控制策略、阈值来源、时序要求和硬件相关限制。
6. 注释风格必须统一；新增代码和修改代码都要带有必要注释，但不要堆砌无信息量注释。
7. 修改接口、参数或控制行为时，优先评估对高速循迹、直角转弯、圆环、菱形、墙面和立体桶场景的影响。
8. 头文件与源文件尽量保持职责清晰，减少无意义的全局变量暴露，但不能以牺牲实时性为代价做过度抽象。
9. 能复用现有逻辑时不要重新实现；能合并的重复逻辑尽量合并。

## C/C++ 注释规范（C89 优先）

- 适用于所有新增或修改的 `C/C++` 代码；本项目固件代码以 C89 和 Keil5 C251 兼容为优先。
- 注释优先解释“为什么这样做”“受什么约束”“改掉会有什么风险”，不要逐行翻译代码。
- 所有新增或修改的函数，必须补中文 `Doxygen` 风格函数头；至少写清 `@brief`、关键参数、返回值；有时序、并发、上下文约束时要额外说明。
- `/** ... */` 仅用于 `Doxygen` 文档注释；实现内部普通说明注释和多行原因说明都优先使用 `//`。
- 关键变量、共享变量、`volatile` 变量、状态位、哨兵值、长度/偏移/单位变量必须解释用途和边界。
- 协议常量、寄存器位、超时、重试次数、阈值、缓冲区大小、采样率、分包长度等魔法数字必须解释来源、单位和含义。
- 复杂代码块必须补“原因说明”注释，重点关注：延时、重试、上电顺序、状态机分支、错误恢复、协议拆包组包、共享状态同步、DMA/Cache 处理、兼容性绕行、降级路径、回滚路径。
- 禁止无信息量注释，如“设置标志位”“调用初始化函数”“计数器加一”“进入循环”。
- 禁止无说明保留被注释掉的旧代码、临时 workaround 或特殊初始化顺序。
- 默认只处理注释；若注释仍无法解决可读性问题，必须先获得用户明确允许，才可做最小重构。

## 性能优先规则

- 高频控制路径优先使用简单、稳定、可预测的实现。
- 能在中断外完成的重计算，不要放进中断。
- 能缓存的结果尽量缓存，避免每周期重复推导。
- 传感器量的限幅、去抖和滤波要尽早处理，但滤波不能明显拖慢响应。
- 调参变量、赛道阈值和状态标志的命名应清晰，方便现场快速排查与调整。

## 多 Agent 协作策略

- 若任务可稳定拆分为多个独立子任务，且子任务之间不共享状态、不同时修改同一文件，并且主 agent 可以独占共享入口、最终集成和最终验证，则默认可启用 Codex 多 agent 工作流，除非用户明确要求单 agent。
- 若任务涉及共享硬件资源、共享状态机、共享配置结构或主链路编排，则默认不启用并行实现，回退为单主 agent。
- 如需显式指定 subagent 配置，默认使用 `gpt-5.4` 与 `high`。
- 若无特殊需要，优先让 subagent 继承主会话配置，不要额外显式传入 `model` 或 `reasoning_effort`。
- 除非用户明确要求，否则不要使用 `gpt-5.1-codex-mini` 或其他降级模型。

## Subagents

- 本仓库按 Codex 当前约定，将项目级自定义 agent 放在 `.codex/agents/`。
- 当前调参专用 agent 为 `speed_loop_tuning`，路径为 `.codex/agents/speed-loop-tuning.toml`。
- 当任务集中在 `project/speed_loop_autotune/` 下的 profile、日志、批次结果、波形摘要和下一步调参建议时，优先使用该 agent。
- 该 agent 只负责调参分析和建议，不默认改固件实时控制逻辑，也不绕过 `enter_ground`、`save` 这类显式用户边界。
- 对 `project/speed_loop_autotune/` 的后续修改，默认遵守以下要求：
  - 调参默认必须走 agent 分析模式，而不是本地 heuristic 自动决策模式。
  - 每一轮填写 PID 之前，都必须先基于当前 `decision_request` 做一次新的 agent 分析。
  - 正常情况下按“分析一轮、执行一轮”往返 10 轮后，再统一向用户汇报。
  - 中途只有失败边界、非法 agent 输出、串口或硬件异常、orchestrator 拒绝消费请求时，才允许提前打断并汇报。
  - 不要在未获得用户明确动作的情况下，自动跨到下一批、自动 `enter_ground` 或自动 `save`。
  - `decision_heuristic.py` 只作为显式 fallback 或调试路径，不能静默替代 `speed_loop_tuning` 成为默认主决策源。
