# AGENTS.md

## 项目背景

- 当前项目基于 STC AI8051U 平台开发。
- 编译器为 Keil5 C251，编辑环境遵循 C89。
- 这是一个高速电磁循迹、差速转向、负压三轮小车项目。
- 小车通过四路电感传感器检测环境中的电磁线，其中包含两路横向和两路竖向传感器。
- 通过 PWM 控制两个电机驱动，实现自动循迹。
- 赛道包含折线、直角转弯、圆环、菱形、直立墙面和立体桶等复杂元素。
- 当前参赛组别为智能车竞赛“飞檐走壁组”。

## 开发目标

- 代码优先保证执行效率和实时响应速度。
- 高速循迹场景下尽量减少延迟、抖动和误判。
- 避免因传感器处理不及时、控制链路过慢而丢失电磁线。
- 在保证性能的前提下，保持模块清晰、注释必要、便于调试。

## 编码与工具约束
先梳理一遍当前项目
- 每一次修改都要检测有没有修改到位
- 所有代码必须符合 C89 标准。
- 禁止使用 C99、C11 或更新标准中的语法与特性。
- 修改代码时优先考虑 Keil5 C251 的兼容性。
可以使用int32
- `AGENTS.md` 与包含中文注释或字符串的源码文件统一使用 UTF-8 编码；若遇到 Keil5 C251 的特殊兼容性问题，以可编译和可读性为先。
- 编译工具路径：`D:\keil_5\UV4\UV4.exe`
- 若任务同时满足以下条件，则默认启用 Codex 多 agent 工作流，除非用户明确要求单 agent：
   - 可稳定拆成至多个独立子任务
   - 子任务不需要共享状态，且不会同时修改同一文件
   - 主 agent 可以独占共享入口、最终集成和最终验证
-若任务涉及共享硬件资源、共享状态机、共享配置结构或主链路编排，默认不启用并行实现，回退为单主 agent。
-默认所有 子spawn_agent 使用 gpt-5.4 与 high
除非用户明确要求，否则不要使用 gpt-5.1-codex-mini 或其他降级模型
如果不需要特殊模型，优先不要显式传 model / reasoning_effort，让 sub agent 继承主会话配置
git提交信息要求中文
## 代码修改要求

1. 确保代码符合 C89 标准，避免使用不兼容特性，例如混合声明与语句、`for (int i = ...)`、`inline`、`stdbool.h`、指定初始化等。
2. 优化循环、分支和数据路径，避免不必要的浮点运算、重复计算、频繁除法、动态分配和其他耗时操作。
3. 及时更新传感器数据，尽量缩短“采样-计算-控制输出”链路，避免因处理延迟导致循迹错误。
4. 控制相关逻辑优先考虑实时性，避免在高频路径中加入打印、阻塞等待或重型调试逻辑。
5. 增加必要且简洁的注释，重点说明状态机、控制策略、阈值来源、时序要求和硬件相关限制。
6. 修改接口、参数或控制行为时，优先评估对高速循迹、直角转弯、圆环、菱形、墙面和立体桶场景的影响。
7. 头文件与源文件尽量保持职责清晰，减少无意义的全局变量暴露，但不能以牺牲实时性为代价做过度抽象。
8. 代码要求注释风格统一，代码要求有注释
9. 谷歌的C语言编程规范
## 性能优先规则

- 高频控制路径优先使用简单、稳定、可预测的实现。
- 能在中断外完成的重计算，不要堆进中断。
- 能缓存的结果尽量缓存，避免每周期重复推导。
- 传感器量的限幅、去抖、滤波要尽早处理，但滤波不能明显拖慢响应。
- 调参变量、赛道阈值和状态标志的命名应清晰，方便现场快速排查与调整。

## 协作说明

- 后续对本仓库的代码分析、重构、优化和修复，都应以上述约束为默认前提。
- 如果某项改动能提升结构，但可能损伤实时性，应优先保留实时性，并明确说明取舍。

## Subagents

- 本仓库按 Codex 官方当前约定，把项目级自定义 agent 放在 `.codex/agents/`。
- 当前调参专用 agent：
  - `speed_loop_tuning`
  - 路径：`.codex/agents/speed-loop-tuning.toml`
- 当任务集中在 `project/speed_loop_autotune/` 的 profile、日志、批次结果、波形摘要和下一步调参建议时，优先使用这个 agent。
- 这个 agent 只负责调参分析和建议，不默认改固件实时控制逻辑，也不绕过 `enter_ground`、`save` 这类显式用户边界。
- 对 `project/speed_loop_autotune/` 的后续修改，默认遵守以下用户要求：
  - 调参默认必须走 agent 分析模式，而不是本地 heuristic 自动决策模式。
  - 每一轮填写 PID 之前，都必须先基于当前 `decision_request` 做一次新的 agent 分析。
  - 正常情况下按“分析一轮、执行一轮”往返 10 轮后，再统一向用户汇报。
  - 中途只有失败边界、非法 agent 输出、串口/硬件异常或 orchestrator 拒绝消费请求时，才允许提前打断并汇报。
  - 不要在未获得用户明确动作的情况下，自动跨到下一批、自动 `enter_ground` 或自动 `save`。
  - `decision_heuristic.py` 只作为显式 fallback / 调试路径，不能静默替代 `speed_loop_tuning` 成为默认主决策源。


读取日志相关的文件放一个log文件夹下，不要到处乱放
头文件通过#include "zf_common_headfile.h"管理
尽量复用现有变量和常数