# AGENTS.md

## 项目背景

- 当前项目基于 STC AI8051U 平台开发。
- 编译器为 Keil5 C251，编辑环境遵循 C89。
- 这是一个高速电磁循迹、差速转向、负压三轮小车项目。
- 小车通过四路电感传感器检测环境中的电磁线，其中包含两路横向和两路竖向传感器。
- 通过 PWM 控制两个电机驱动，实现自动循迹。
- 赛道包含折线、直角转弯、圆环、菱形、直立墙面和立体桶等复杂元素。
- 当前参赛组别为智能车竞赛“飞檐走壁组”。

## 开发目标

- 代码优先保证执行效率和实时响应速度。
- 高速循迹场景下尽量减少延迟、抖动和误判。
- 避免因传感器处理不及时、控制链路过慢而丢失电磁线。
- 在保证性能的前提下，保持模块清晰、注释必要、便于调试。

## 编码与工具约束

- 所有代码必须符合 C89 标准。
- 禁止使用 C99、C11 或更新标准中的语法与特性。
- 修改代码时优先考虑 Keil5 C251 的兼容性。
- `AGENTS.md` 与包含中文注释或字符串的源码文件统一使用 UTF-8 编码；若遇到 Keil5 C251 的特殊兼容性问题，以可编译和可读性为先。
- 编译工具路径：`D:\keil_5\UV4\UV4.exe`

## 代码修改要求

1. 确保代码符合 C89 标准，避免使用不兼容特性，例如混合声明与语句、`for (int i = ...)`、`inline`、`stdbool.h`、指定初始化等。
2. 优化循环、分支和数据路径，避免不必要的浮点运算、重复计算、频繁除法、动态分配和其他耗时操作。
3. 及时更新传感器数据，尽量缩短“采样-计算-控制输出”链路，避免因处理延迟导致循迹错误。
4. 控制相关逻辑优先考虑实时性，避免在高频路径中加入打印、阻塞等待或重型调试逻辑。
5. 增加必要且简洁的注释，重点说明状态机、控制策略、阈值来源、时序要求和硬件相关限制。
6. 修改接口、参数或控制行为时，优先评估对高速循迹、直角转弯、圆环、菱形、墙面和立体桶场景的影响。
7. 头文件与源文件尽量保持职责清晰，减少无意义的全局变量暴露，但不能以牺牲实时性为代价做过度抽象。

## 性能优先规则

- 高频控制路径优先使用简单、稳定、可预测的实现。
- 能在中断外完成的重计算，不要堆进中断。
- 能缓存的结果尽量缓存，避免每周期重复推导。
- 传感器量的限幅、去抖、滤波要尽早处理，但滤波不能明显拖慢响应。
- 调参变量、赛道阈值和状态标志的命名应清晰，方便现场快速排查与调整。

## 协作说明

- 后续对本仓库的代码分析、重构、优化和修复，都应以上述约束为默认前提。
- 如果某项改动能提升结构，但可能损伤实时性，应优先保留实时性，并明确说明取舍。