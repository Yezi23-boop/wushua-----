# 代码体积优化计划：code 从 61749 降至 ~60000

## 目标
将 code 段从 61749 字节降至 ~60000 字节（减少约 1750 字节）。

## 优化方案（按收益排序）

### 优化 1：裁剪 vofa.c + zf_device_wireless_uart.c + debug_view.c（~1500 字节）
**收益最大，是最核心的优化项。**

当前 `MAIN_ENABLE_VOFA=0`、`MAIN_ENABLE_SPEED_TEST=0`，这三个模块的函数全部处于禁用状态，但编译器仍然将它们编译进 code 段。

**具体步骤：**

1. **从 Keil 工程中移除三个 `.c` 文件**（不删除磁盘文件，只从 seekfree.uvproj 中排除）：
   - `project/service/debug_view.c`（+`.h`）
   - `project/service/vofa.c`（+`.h`）
   - `libraries/zf_device/zf_device_wireless_uart.c`（+`.h`）

2. **`project/user/int_user.c`**：注释掉 `wireless_uart_init()` 调用（第 63 行）

3. **`libraries/zf_common/zf_common_headfile.h`**：注释掉三行 include：
   - `#include "zf_device_wireless_uart.h"`（第 80 行）
   - `#include "vofa.h"`（第 96 行）
   - `#include "debug_view.h"`（第 107 行）

**关键文件：**
- `project/mdk/seekfree.uvproj`
- `project/user/int_user.c:63`
- `libraries/zf_common/zf_common_headfile.h:80,96,107`

### 优化 2：删除未使用的 ascii_font_6x8 字库（~576 字节）
`ascii_font_6x8[96][6]`（576 字节）存放在 code 段，全项目零调用。

**具体步骤：**

1. **`libraries/zf_common/zf_common_font.c`**：删除第 137~248 行的 `ascii_font_6x8` 数组定义
2. **`libraries/zf_common/zf_common_font.h`**：删除第 62 行的 `extern` 声明

**关键文件：**
- `libraries/zf_common/zf_common_font.c:137-248`
- `libraries/zf_common/zf_common_font.h:62`

### 优化 3（补充）：menu.c 合并重复初始化块（~50 字节）
`Menu_Set_Service_Enable()` 的 if/else 两个分支代码完全相同。

**具体步骤：**

- **`project/service/menu.c:98-122`**：将 if/else 合并为无条件执行

## 预期收益

| 优化项 | 预估节省 |
|--------|----------|
| 裁剪 vofa + wireless_uart + debug_view | ~1500 字节 |
| 删除 ascii_font_6x8 | ~576 字节 |
| menu.c 合并重复 | ~50 字节 |
| **合计** | **~2100 字节** |

预计优化后 code ≈ 59600~60000，满足目标。

## 验证方法
1. 编译：`& 'D:\Keil_v5\UV4\UV4.exe' -b 'project\mdk\seekfree.uvproj'`
2. 读取 `project/mdk/out_file/SEEKFREE.build_log.htm` 确认 0 Error、0 Warning
3. 确认 code 段 ≤ 60000
4. 运行现有测试：`python -m pytest tests/ -v`（确保无回归）
